"""Rebuilds mod01_01_introducere.json with valid JSON using Python's json.dumps."""
import json, pathlib

ROOT = pathlib.Path(__file__).parent.parent
OUT  = ROOT / "backend/lessons/mod01_bazele/mod01_01_introducere.json"

data = {
  "id": "mod01_01",
  "titleKey": "lessons.mod01_01",
  "interactiveComponent": "MissionKernelCout",
  "theory": {
    "ro": {
      "cpp": {
        "architect": (
          "### >_ [SYSTEM SPEC]: Structura unui program C++\n\n"
          "Un program C++ parcurge patru etape: **preprocesare**, **compilare**, **linking** si **executie**. "
          "Preprocesorul copiaza declaratiile din `<iostream>` in unitatea de compilare. "
          "Compilatorul traduce `.cpp` in cod obiect `.o`. "
          "Linker-ul combina obiectele si produce executabilul.\n\n"
          "Fiecare program C++ necesita exact o functie `main()` cu semnatura `int main()`. "
          "Lipsa ei genereaza `undefined reference to main`. "
          "Fiecare instructiune se termina cu `;` -- omiterea produce `error: expected ; before [token]`.\n\n"
          "Obiectul `cout` este un stream de iesire conectat la `stdout`. "
          "Operatorul de insertie `<<` trimite date in stream. "
          "Operatorul corect este `<<`, nu `>>` -- `cout >> text` nu compileaza, `>>` apartine lui `cin`. "
          "Absenta `#include <iostream>` genereaza `error: cout was not declared in this scope`.\n\n"
          "```cpp\n"
          "#include <iostream>\n"
          "using namespace std;\n\n"
          "int main() {\n"
          "    cout << \"Primul program!\" << \"\\n\";\n"
          "    cout << \"Tab: \\t | Newline: \\n\" << \"\\n\";\n"
          "    return 0;\n"
          "}\n"
          "```\n\n"
          "**Avertisment tehnic:** `endl` insereaza newline si apeleaza `flush()` pe buffer -- "
          "operatie I/O separata, costisitoare in bucle mari. "
          "Preferati `'\\n'` in probleme de concurs cu output voluminos. "
          "Ghilimelele nechise produc `error: missing terminating character`. "
          "Evitati `using namespace std;` in fisiere header -- preferati `std::cout` explicit pentru portabilitate."
        ),
        "hacker": (
          "### >_ [ALERT]: Pipeline C++ -- patru etape pana la executie\n\n"
          "Programul tau nu ruleaza direct din sursa. Trece prin: preprocesare -> compilare -> linking -> executie. "
          "Prima etapa activeaza la `#include <iostream>` -- injectand declaratiile stream-ului de I/O. "
          "Compilatorul emite cod obiect. Linker-ul asambleaza binar executabil cu un singur entry point: `main()`.\n\n"
          "Fara `main()` -- linker-ul arunca `undefined reference to main` si executia nu porneste. "
          "Fiecare instructiune necesita `;` la final -- compilatorul opreste cu `expected ; before [token]`. "
          "Bug clasic: `cout >> text` in loc de `cout << text`. "
          "Operatorul `>>` e al lui `cin` -- directie inversata, eroare de compilare.\n\n"
          "Alt fail frecvent: `#include <iostream>` lipsa -- `cout was not declared in this scope`. "
          "Ghilimele deschise fara pereche -- `missing terminating character`. "
          "Secventele de escape `'\\n'` si `'\\t'` sunt procesate la build-time.\n\n"
          "```cpp\n"
          "#include <iostream>\n"
          "using namespace std;\n\n"
          "int main() {\n"
          "    cout << \"Output activat.\" << \"\\n\";\n"
          "    cout << \"Escape: tab=\\t newline=\\n\" << \"\\n\";\n"
          "    return 0;\n"
          "}\n"
          "```\n\n"
          "**Optimizare competitiva:** `endl` forteaza flush la fiecare apel -- in bucle cu mii de iteratii, "
          "latenta se acumuleaza. Inlocuiti cu `'\\n'`. Combinati cu `ios::sync_with_stdio(false)` si "
          "`cin.tie(NULL)` pentru viteza maxima -- dezactiveaza sincronizarea C/C++ si poate injumatati "
          "timpul de I/O pe input-uri mari."
        ),
        "socrates": (
          "### >_ [QUERY]: De ce programul tau are nevoie de un contract cu OS-ul?\n\n"
          "Gandeste-te: de ce compilatorul refuza instructiuni scrise direct in fisier, in afara oricarei functii? "
          "Sistemul de operare are nevoie de un contract explicit -- o adresa de start bine definita. "
          "Acel contract se numeste `main()`.\n\n"
          "Inainte ca OS-ul sa apeleze `main()`, codul tau trece prin trei filtre: "
          "preprocesorul (extinde `#include <iostream>` in declaratii concrete), "
          "compilatorul (transforma C++ in cod masina), "
          "linker-ul (combina totul intr-un executabil). "
          "Daca uiti `;` la finalul unei instructiuni -- compilatorul opreste cu `expected ; before [token]`.\n\n"
          "De ce `cout << text` si nu `cout >> text`? "
          "Gandeste-te la directie: `<<` inseamna trimite spre stanga, spre canal. "
          "`>>` inseamna extrage din canal spre variabila -- asta e operatia lui `cin`. "
          "Schimbarea directiei este o eroare de compilare imediata.\n\n"
          "```cpp\n"
          "#include <iostream>\n"
          "using namespace std;\n\n"
          "int main() {\n"
          "    cout << \"Buna ziua!\" << \"\\n\";\n"
          "    cout << \"Tab: \\t si newline: \\n incluse.\" << \"\\n\";\n"
          "    return 0;\n"
          "}\n"
          "```\n\n"
          "**Insight tehnic:** `endl` este echivalent cu `'\\n'` plus `flush()` -- forteaza scrierea imediata "
          "a buffer-ului, util in debugging, costisitor in productie. "
          "Absenta `#include <iostream>` face `cout` invizibil pentru compilator; "
          "ghilimelele nechise produc erori de tokenizare. "
          "Semicolonul nu este optional -- C++ delimiteaza instructiunile prin `;`, "
          "nu prin indentare sau newline, spre deosebire de Python."
        ),
      },
      "python": {
        "architect": (
          "### >_ [SYSTEM SPEC]: Structura unui program Python\n\n"
          "Python este un limbaj interpretat: CPython citeste fisierul `.py` linie cu linie, "
          "compileaza fiecare instructiune la bytecode si o executa imediat in Python Virtual Machine (PVM). "
          "Nu exista faza de linking; importurile sunt rezolvate la runtime via `sys.path`.\n\n"
          "Executia incepe de la prima linie a fisierului si continua secvential. "
          "Nu exista functie de entry point obligatorie. "
          "Conventia `if __name__ == '__main__':` separa codul de executie directa de cel importabil ca modul "
          "-- `__name__` primeste valoarea `'__main__'` doar la rulare directa, nu la import.\n\n"
          "Indentarea este **sintaxa obligatorie**, nu stil. "
          "Un bloc indentat gresit genereaza `IndentationError: unexpected indent`. "
          "Amestecul de tab-uri si spatii produce `TabError`. "
          "Functia `print()` scrie pe `stdout` si adauga automat `'\\n'`. "
          "Parametrul `end=''` controleaza terminatorul; `sep=','` controleaza separatorul. "
          "Ghilimelele nechise produc `SyntaxError: EOL while scanning string literal`. "
          "Semicolonul la final este legal, dar violeaza PEP8.\n\n"
          "```python\n"
          "print(\"Primul program Python!\")\n"
          "print(\"Tab: \\t | Newline: \\n\")\n"
          "print(\"A\", \"B\", \"C\", sep=\"-\", end=\"!\\n\")\n"
          "```\n\n"
          "**Avertisment tehnic:** `IndentationError` este una dintre cele mai frecvente erori "
          "pentru studentii care migreaza din C++. "
          "Python nu accepta amestecul tab+spatii in indentare -- produce `TabError`. "
          "Ghilimelele simple si duble sunt interschimbabile, dar nu se pot amesteca in acelasi string literal. "
          "Adaugarea `;` la final nu produce eroare, dar semnaleaza ca autorul nu intelege semantica Python."
        ),
        "hacker": (
          "### >_ [ALERT]: Python -- executie directa, zero etape de compilare\n\n"
          "Target: executie directa, fara build steps. "
          "Vector: interpretorul CPython, care proceseaza fiecare linie ca instructiune autonoma. "
          "Fisierul `.py` este atat sursa cat si executabil -- nu exista separare intre compilare si runtime.\n\n"
          "`print()` este comanda de output. Fara argumente suplimentare, adauga `'\\n'` automat. "
          "`end=''` suprima newline-ul pentru output continuu; `sep=','` seteaza separatorul. "
          "Guard-ul `if __name__ == '__main__':` activeaza blocul de executie doar in modul script direct "
          "-- dezactivat la import.\n\n"
          "Pitfall-uri operative: ghilimele deschise fara pereche -- `SyntaxError: EOL while scanning string literal`. "
          "Indentare incorecta -- `IndentationError: unexpected indent`. "
          "Tab+spatii amestecate -- `TabError`. "
          "Semicolonul final nu blocheaza executia, dar marcheaza cod C++ tradus naiv in Python.\n\n"
          "```python\n"
          "if __name__ == '__main__':\n"
          "    print(\"Output activat.\")\n"
          "    print(\"Multi-arg:\", \"A\", \"B\", sep=\" | \", end=\"\\n\")\n"
          "    print(\"Escape: tab=\\t newline=\\n\")\n"
          "```\n\n"
          "**Optimizare competitiva:** `print()` in Python are overhead semnificativ per apel fata de `cout` "
          "-- fiecare invocare implica lookup dinamic, formatare si apel de sistem. "
          "Pentru probleme cu output masiv, colectati rezultatele intr-un `list` si emiteti un singur "
          "`sys.stdout.write('\\n'.join(results))`. "
          "Aceasta reduce apelurile de sistem de la N la 1, taind substantial timpul de I/O."
        ),
        "socrates": (
          "### >_ [QUERY]: De ce Python executa codul fara sa stie de unde sa inceapa?\n\n"
          "O intrebare inainte de cod: cum stie interpretorul Python de unde sa inceapa executia, "
          "daca nu exista o functie `main()` obligatorie?\n\n"
          "Raspunsul: nu are nevoie sa stie. Python executa fisierul de sus in jos, instructiune cu instructiune. "
          "OS-ul porneste interpretorul si ii da fisierul; interpretorul citeste linie cu linie. "
          "Primul `print()` din fisier se executa primul.\n\n"
          "Atunci de ce exista `if __name__ == '__main__':`? "
          "Gandeste-te la un instrument care poate fi folosit singur (ca script) "
          "sau ca parte dintr-un ansamblu (ca modul importat). "
          "Cand importi un modul, nu vrei ca tot codul sa ruleze automat -- vrei doar sa incarci functiile. "
          "Guard-ul face exact aceasta diferentiere.\n\n"
          "Ce se intampla daca indentezi gresit? "
          "Python trateaza indentarea ca sintaxa -- nu ca stil. "
          "Un bloc indentat la nivel gresit genereaza `IndentationError`. "
          "Ghilimele deschise fara pereche produc `SyntaxError: EOL while scanning string literal`.\n\n"
          "```python\n"
          "def salut():\n"
          "    print(\"Functie apelata explicit.\")\n\n"
          "if __name__ == '__main__':\n"
          "    salut()\n"
          "    print(\"Executat direct, nu importat.\")\n"
          "```\n\n"
          "**Insight tehnic:** Spre deosebire de C++, unde un `;` lipsa opreste compilarea, "
          "Python foloseste newline-ul ca terminator implicit de instructiune -- "
          "niciun delimiter explicit nu este necesar. "
          "Adaugarea unui `;` la final este sintactic legala, dar idiomatically incorecta "
          "si tradeaza un obicei din C++. "
          "`IndentationError` si `TabError` sunt unice Python-ului: "
          "fiecare nivel de bloc trebuie indentat consistent -- "
          "interpretorul refuza executia la prima inconsistenta."
        ),
      },
    },
    "en": {
      "cpp": {
        "architect": (
          "### >_ [SYSTEM SPEC]: Structure of a C++ Program\n\n"
          "A C++ program passes through four stages before execution: **preprocessing**, **compilation**, "
          "**linking**, and **execution**. The preprocessor copies declarations from the `<iostream>` header "
          "into the translation unit. The compiler translates `.cpp` source into object code `.o`. "
          "The linker combines objects into the final executable binary.\n\n"
          "Every C++ program requires exactly one `main()` function with signature `int main()`. "
          "Its absence generates `undefined reference to main`. "
          "Every statement must be terminated with `;` -- omitting it produces `error: expected ; before [token]`.\n\n"
          "The `cout` object is an output stream connected to `stdout`. "
          "The insertion operator `<<` sends data into the stream. "
          "The correct operator is `<<`, not `>>` -- `cout >> text` does not compile; `>>` belongs to `cin`. "
          "Missing `#include <iostream>` produces `error: cout was not declared in this scope`.\n\n"
          "```cpp\n"
          "#include <iostream>\n"
          "using namespace std;\n\n"
          "int main() {\n"
          "    cout << \"First program!\" << \"\\n\";\n"
          "    cout << \"Tab: \\t | Newline: \\n\" << \"\\n\";\n"
          "    return 0;\n"
          "}\n"
          "```\n\n"
          "**Technical warning:** `endl` inserts a newline and calls `flush()` on the buffer -- "
          "a separate I/O operation, expensive inside tight loops. "
          "Prefer `'\\n'` in competitive programming with heavy output. "
          "Unclosed string literals produce `error: missing terminating character`. "
          "Avoid `using namespace std;` in header files -- prefer explicit `std::cout` for portability."
        ),
        "hacker": (
          "### >_ [ALERT]: C++ Pipeline -- Four Stages to Execution\n\n"
          "Your program does not run directly from source. "
          "It passes through: preprocessing -> compilation -> linking -> execution. "
          "The first stage fires with `#include <iostream>` -- injecting I/O stream declarations. "
          "The compiler emits object code. The linker assembles an executable binary with a single entry point: `main()`.\n\n"
          "Without `main()` -- the linker throws `undefined reference to main` and execution never starts. "
          "Every statement requires a trailing `;` -- the compiler stops with `expected ; before [token]`. "
          "Classic bug: `cout >> text` instead of `cout << text`. "
          "The `>>` operator belongs to `cin` -- reversed direction, immediate compile error.\n\n"
          "Other common fails: missing `#include <iostream>` -- `cout was not declared in this scope`. "
          "Unclosed string literal -- `missing terminating character`. "
          "Escape sequences `'\\n'` and `'\\t'` are processed at build-time, not runtime.\n\n"
          "```cpp\n"
          "#include <iostream>\n"
          "using namespace std;\n\n"
          "int main() {\n"
          "    cout << \"Output activated.\" << \"\\n\";\n"
          "    cout << \"Escapes: tab=\\t newline=\\n\" << \"\\n\";\n"
          "    return 0;\n"
          "}\n"
          "```\n\n"
          "**Competitive optimization:** `endl` forces a buffer flush on every call -- "
          "in loops with thousands of iterations, latency accumulates. Replace with `'\\n'`. "
          "Combine with `ios::sync_with_stdio(false)` and `cin.tie(NULL)` for maximum stream throughput "
          "-- this disables C/C++ stream synchronization and can halve I/O time on large inputs."
        ),
        "socrates": (
          "### >_ [QUERY]: Why does your program need a contract with the OS?\n\n"
          "Consider: why does the compiler reject instructions written directly in a file, outside any function? "
          "The operating system needs an explicit contract -- a well-defined start address. "
          "That contract is called `main()`.\n\n"
          "Before the OS calls `main()`, your code passes through three filters: "
          "the preprocessor (expands `#include <iostream>` into concrete declarations), "
          "the compiler (transforms C++ into machine code), "
          "and the linker (combines everything into an executable). "
          "Forget `;` at the end of a statement and the compiler stops with `expected ; before [token]`.\n\n"
          "Why `cout << text` and not `cout >> text`? "
          "Think about direction: `<<` means send toward the left, toward the channel. "
          "`>>` means extract from the channel into a variable -- that is `cin`. "
          "Reversing the direction is an immediate compile error.\n\n"
          "```cpp\n"
          "#include <iostream>\n"
          "using namespace std;\n\n"
          "int main() {\n"
          "    cout << \"Hello!\" << \"\\n\";\n"
          "    cout << \"Tab: \\t and newline: \\n included.\" << \"\\n\";\n"
          "    return 0;\n"
          "}\n"
          "```\n\n"
          "**Technical insight:** `endl` is equivalent to `'\\n'` plus `flush()` -- "
          "forces immediate writing of the output buffer, useful for real-time debugging, costly in production loops. "
          "Missing `#include <iostream>` makes `cout` invisible to the compiler; "
          "unclosed string literals cause tokenization errors. "
          "The semicolon is not optional -- C++ delimits statements with `;`, "
          "not indentation or newlines, unlike Python."
        ),
      },
      "python": {
        "architect": (
          "### >_ [SYSTEM SPEC]: Structure of a Python Program\n\n"
          "Python is an interpreted language: CPython reads the `.py` file line by line, "
          "compiles each statement to bytecode, and executes it immediately in the Python Virtual Machine (PVM). "
          "There is no linking phase; imports are resolved at runtime via `sys.path`.\n\n"
          "Execution begins at the first line of the file and proceeds sequentially. "
          "There is no mandatory entry-point function. "
          "The convention `if __name__ == '__main__':` separates direct-execution code from importable module code "
          "-- `__name__` receives the value `'__main__'` only when the file is run directly, not when imported.\n\n"
          "Indentation is **mandatory syntax**, not style. "
          "A misaligned block produces `IndentationError: unexpected indent`. "
          "Mixing tabs and spaces produces `TabError`. "
          "The `print()` function writes to `stdout` and appends `'\\n'` automatically. "
          "The `end=''` parameter controls the terminator; `sep=','` controls the separator. "
          "Unclosed string literals produce `SyntaxError: EOL while scanning string literal`. "
          "A trailing semicolon is legal but violates PEP8.\n\n"
          "```python\n"
          "print(\"First Python program!\")\n"
          "print(\"Tab: \\t | Newline: \\n\")\n"
          "print(\"A\", \"B\", \"C\", sep=\"-\", end=\"!\\n\")\n"
          "```\n\n"
          "**Technical warning:** `IndentationError` is among the most frequent errors for students migrating from C++. "
          "Python does not accept mixed tabs and spaces in indentation -- mixing produces `TabError`. "
          "Single and double quotes are interchangeable but cannot be mixed within the same string literal. "
          "A trailing `;` does not cause an error but signals the author does not understand Python statement semantics."
        ),
        "hacker": (
          "### >_ [ALERT]: Python Interpreter -- Direct Execution, Zero Build Steps\n\n"
          "Target: direct execution, no build pipeline. "
          "Vector: the CPython interpreter, which processes each line as an independent statement. "
          "The `.py` file is both source and executable -- no distinction between compilation and runtime.\n\n"
          "`print()` is the output command. Without extra arguments it appends `'\\n'` automatically. "
          "`end=''` suppresses the newline for continuous output; `sep=','` sets the separator. "
          "The `if __name__ == '__main__':` guard activates the execution block only in direct-script mode "
          "-- disabled on import.\n\n"
          "Operational pitfalls: unclosed string literal -- `SyntaxError: EOL while scanning string literal`. "
          "Incorrect indentation -- `IndentationError: unexpected indent`. "
          "Mixed tabs and spaces -- `TabError`. "
          "Trailing semicolons do not crash execution but mark naively translated C++ code.\n\n"
          "```python\n"
          "if __name__ == '__main__':\n"
          "    print(\"Output activated.\")\n"
          "    print(\"Multi-arg:\", \"A\", \"B\", sep=\" | \", end=\"\\n\")\n"
          "    print(\"Escapes: tab=\\t newline=\\n\")\n"
          "```\n\n"
          "**Competitive optimization:** `print()` in Python carries significant per-call overhead compared to `cout` "
          "-- each invocation involves dynamic lookup, formatting, and a system call. "
          "For problems with massive output, collect results in a list and emit a single "
          "`sys.stdout.write('\\n'.join(results))`. "
          "This reduces system calls from N to 1, cutting I/O time substantially on judge systems with tight time limits."
        ),
        "socrates": (
          "### >_ [QUERY]: Why does Python run code without knowing where to start?\n\n"
          "Before writing any code, a question: how does the Python interpreter know where to begin execution "
          "if there is no mandatory `main()` function?\n\n"
          "The answer: it does not need to know. Python executes the file from top to bottom, instruction by instruction. "
          "The OS starts the interpreter and hands it the file; the interpreter reads line by line. "
          "The first `print()` in the file executes first.\n\n"
          "So why does `if __name__ == '__main__':` exist? "
          "Think of a tool that can work standalone as a script, "
          "or as part of a larger system when imported as a module. "
          "When you import a module, you do not want all its code to run automatically -- "
          "you only want to load the functions. The guard makes exactly this distinction.\n\n"
          "What happens if you indent incorrectly? "
          "Python treats indentation as syntax, not style. "
          "A block indented at the wrong level produces `IndentationError`. "
          "Unclosed quotes produce `SyntaxError: EOL while scanning string literal`.\n\n"
          "```python\n"
          "def greet():\n"
          "    print(\"Function called explicitly.\")\n\n"
          "if __name__ == '__main__':\n"
          "    greet()\n"
          "    print(\"Run directly, not imported.\")\n"
          "```\n\n"
          "**Technical insight:** Unlike C++, where a missing `;` stops compilation, "
          "Python uses newlines as implicit statement terminators -- no explicit delimiter needed. "
          "A trailing `;` is syntactically valid but idiomatically wrong and reveals a C++ habit. "
          "`IndentationError` and `TabError` are unique to Python: "
          "every block level must be indented consistently -- "
          "the interpreter refuses execution at the first indentation inconsistency it encounters."
        ),
      },
    },
  },
  "quiz": {
    "question_ro": "Care este rolul instructiunii `return 0;` la sfarsitul functiei `main()` in C++?",
    "question_en": "What is the role of the `return 0;` statement at the end of the `main()` function in C++?",
    "options_ro": [
      "Goleste (flush) buffer-ul de iesire inainte de terminarea programului",
      "Transmite codul de iesire sistemului de operare, indicand executie cu succes",
      "Dezaloca toata memoria heap utilizata de program",
      "Reporneste executia functiei main() de la inceput"
    ],
    "options_en": [
      "Flushes the output buffer before the program terminates",
      "Transmits the exit code to the operating system, indicating successful execution",
      "Deallocates all heap memory used by the program",
      "Restarts the execution of main() from the beginning"
    ],
    "correctAnswerIndex": 1,
    "explanation_ro": (
      "`return 0` transmite codul de iesire procesului parent (shell sau OS). "
      "Valoarea `0` semnifica executie cu succes conform conventiei POSIX/ISO C++. "
      "Memoria heap este eliberata de OS la terminarea procesului; "
      "flush-ul buffer-ului se face separat prin `endl` sau `fflush`. "
      "Valorile non-zero semnalizeaza erori si pot fi detectate de procese parent sau scripturi shell."
    ),
    "explanation_en": (
      "`return 0` transmits the exit code to the parent process (shell or OS). "
      "The value `0` signifies successful execution per the POSIX/ISO C++ convention. "
      "Heap memory is freed by the OS when the process terminates; "
      "buffer flushing is handled separately via `endl` or `fflush`. "
      "Non-zero values signal errors and can be detected by parent processes or shell scripts."
    ),
  },
}

out = json.dumps(data, ensure_ascii=False, indent=2)
OUT.write_text(out, encoding="utf-8")
print(f"Written {len(out)} bytes to {OUT}")

# Validate
parsed = json.loads(out)
assert parsed["id"] == "mod01_01"
assert set(parsed["theory"].keys()) == {"ro", "en"}
assert set(parsed["theory"]["ro"]["cpp"].keys()) == {"architect", "hacker", "socrates"}
assert set(parsed["theory"]["en"]["python"].keys()) == {"architect", "hacker", "socrates"}
print("JSON is valid — all 12 theory blocks present")
